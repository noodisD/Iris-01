[project]
name = "iris-minimal"
version = "0.1.0"
description = "IRIS: a local-first AI companion for one person, built on a deterministic analytical spine"
authors = [{name = "noodis", email = "noodis@users.noreply.github.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11,<3.15"

dependencies = [
    # Core LLM
    "openai>=1.50.0",

    # Data & Validation
    "pydantic>=2.8.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0",

    # Databases
    "psycopg2-binary>=2.9.0",
    "pgvector>=0.2.5",

    # ML / Clustering
    "scikit-learn>=1.5.0",
    "numpy>=2.1.0",

    # Core application dependencies
    "typing-extensions>=4.8.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "python-multipart>=0.0.12",
    "tenacity>=9.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-mock>=3.14.0",
    "black>=23.0.0",
    "mypy>=1.0.0",
    "ruff>=0.1.6",
]

[project.scripts]
iris = "companion:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# The application is the API module, the CLI and the prompts, not just the
# analytical package. Shipping only "agent" produced a wheel whose console
# script (iris = companion:main) and whose prompts import both failed at
# runtime.
include = ["agent", "prompts", "iris_api.py", "companion.py"]

[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "pytest-mock>=3.14.0",
    "black>=23.0.0",
    "mypy>=1.0.0",
    "ruff>=0.1.6",
]

[tool.black]
line-length = 100
target-version = ['py311']

[tool.mypy]
# The version .python-version pins and CI runs. black, mypy and ruff said 3.14
# while everything ran on 3.11, so each tool checked a language nothing used.
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
# Checked strictly, and gating in CI. An allow-list rather than the whole
# package: the rest is not typed to this standard yet, and a typechecker that
# runs over everything and is ignored teaches people to ignore it. A module
# joins by passing; the list only grows.
follow_imports = "silent"
files = [
    "agent/analysis_cache.py",
    "agent/conflicts.py",
    "agent/coverage.py",
    "agent/importing/dates.py",
    "agent/narrative_policy.py",
    "agent/narrative_templates.py",
    "agent/pipeline_orchestrator.py",
    "agent/review_letter.py",
    "agent/timeutils.py",
    "agent/work_queue.py",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["archive", "frontend"]

[tool.ruff.lint]
# Moved out of [tool.ruff]: top-level select/ignore have been deprecated since
# ruff 0.2 and were emitting a warning on every run.
select = ["E", "W", "F", "I", "B", "C4", "C9", "DTZ", "G", "PGH", "PIE", "PL", "PT", "PYI", "RUF", "SIM", "UP", "EXE"]
ignore = [
    "PLR",   # pylint "design" rules: argument/branch/statement counts
    "E501",  # line length is handled by the formatter, not the linter
]

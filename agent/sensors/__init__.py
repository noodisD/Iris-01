"""Sensor sources — measured evidence, not written reflections.

A sensor reading is dated, attributable, and low-weight; it never
becomes a theme on its own. The package mirrors agent/importing/'s
shape: adapters parse, transport fetches, service commits.
"""
